Budget and planning

create a budget to get notified when it exceeds

Amazon Bedrock

Fastest growing Gen AI service in aws currently.

GenAI is subset of Deep learning, used to generate new data.

feed lots of unlabelled data to foundational pre trined models.
The models may cost millions of dollar

GPT-4o is foundation model behimd ChatGPT

LLMs(large language Models)- can generate human like text


