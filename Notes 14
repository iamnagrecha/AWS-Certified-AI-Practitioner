Terms IMP for Exam

GPT (generative pre-trained transformer)
BERT(Bidirectional encoder representations from transformers)- similar intent to GPT, but reads text in two directions
GAN (generative adversarial network)- genrates synthetic data like images, vidos or sound, for data augmentation
ResNET (Residual Network- part of deep learning)- for images
SVM (support vector machines)- ML algo

Labelled data can be costly, so most data is unlabelled

Structured data- excel, ros and columns
unstructured data -text or images
